#include <stdio.h>
int main()
 {
 	int input;
 	printf("Enter a positive number:");
    scanf("%d",&input);
    if(input%2==0)
    {
    	printf("Even");
    } else
     {
     	printf("Odd");
     }
    return 0;
}
